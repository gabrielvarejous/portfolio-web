---
const { title, subtitle } = Astro.props;
---

<di class="mb-12 text-center">
    <h2 class="text-3xl font-bold text-white">{title}</h2>
    {subtitle && (
        <p class="mt-4 text-white/60">{subtitle}</p>
    )}
</div>